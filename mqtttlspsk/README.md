# Connecting to a MQTT broker using TLS-PSK

The connection is TLS-PSK with a simple shared key authentication mode, no certificates nor private keys.

See [my blog]() for further info.

Más información en [mi blog]()
